import type { Operation } from "./operation";

export const OperationIconMap = new Map<Operation, string>([
	["addition", "+"],
	["subtraction", "–"],
	["multiplication", "×"],
	["division", "÷"],
]);
